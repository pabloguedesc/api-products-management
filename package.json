{
  "private": true,
  "scripts": {
    "dev": "php artisan config:cache && php artisan config:clear && php artisan migrate &&php artisan db:seed && php artisan serve",
    "controller": "php artisan make:controller"
  }
}
